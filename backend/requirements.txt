# Backend dependencies. The shared feature package is installed separately:
#   pip install -e .   (from the repo root)
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.6.1
SQLAlchemy==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
slowapi==0.1.9
prometheus-client==0.21.1
redis==5.2.1  # optional: shared /check cache across replicas (see REDIS_URL)
# --- ML runtime (versions pinned to the training environment for
#     pickle compatibility -- keep in sync with ml_pipeline/requirements.txt) ---
joblib==1.4.2
numpy==2.1.3
scikit-learn==1.5.2
xgboost==2.1.3
Levenshtein==0.26.1
python-whois==0.9.5
httpx>=0.27
python-jose[cryptography]==3.3.0
passlib==1.7.4
# passlib 1.7.4 reads bcrypt.__about__.__version__, removed in bcrypt 4.1+.
# Unpinned, a fresh install pulls bcrypt 5.x and login crashes with
# "password cannot be longer than 72 bytes" / AttributeError. Pin to the
# last 4.0.x that passlib 1.7.4 supports.
bcrypt==4.0.1
